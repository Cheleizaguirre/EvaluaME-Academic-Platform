namespace Mudul.Models
{
    public class JobeLanguage
    {
        // Se asume que la API retorna un objeto con estas propiedades (ajusta según corresponda)
        public string language_id { get; set; }
        public string version { get; set; }
    }
}
